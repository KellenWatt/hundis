<%= form_for(tournament_language) do |f| %>
  <% if tournament_language.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tournament_language.errors.count, "error") %> prohibited this tournament_language from being saved:</h2>

      <ul>
      <% tournament_language.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
