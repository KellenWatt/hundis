<%= form_for(user_submission) do |f| %>
  <% if user_submission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_submission.errors.count, "error") %> prohibited this user_submission from being saved:</h2>

      <ul>
      <% user_submission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
